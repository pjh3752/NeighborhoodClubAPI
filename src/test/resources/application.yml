spring:
  jpa:
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  h2:
    console:
      enabled: true

  security:
    oauth2:
      client:
        registration:
          google:
            clientId: test
            clientSecret: test
            scope:
              - email
              - profile